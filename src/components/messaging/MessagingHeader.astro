<!-- Messaging Header -->
<section class="messaging-header messaging-bg">
    <div class="container">
        <h1 class="messaging-header-title">Secure Professional Messaging</h1>
        <p class="messaging-header-subtitle">Connect with cleared professionals, recruiters, and hiring managers in a secure environment</p>

        <button class="btn-compose-new">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            Compose New Message
        </button>

        <div class="messaging-quick-stats">
            <div class="msg-stat">
                <svg class="msg-stat-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
                </svg>
                <span class="msg-stat-value">12</span>
                <span class="msg-stat-label">Active Conversations</span>
            </div>
            <div class="msg-stat">
                <svg class="msg-stat-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="3" y="11" width="18" height="11" rx="2" ry="2"/>
                    <path d="M7 11V7a5 5 0 0 1 10 0v4"/>
                </svg>
                <span class="msg-stat-value">AES-256</span>
                <span class="msg-stat-label">End-to-End Encrypted</span>
            </div>
            <div class="msg-stat">
                <svg class="msg-stat-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <polyline points="20 6 9 17 4 12"/>
                </svg>
                <span class="msg-stat-value">Real-Time</span>
                <span class="msg-stat-label">Read Receipts</span>
            </div>
        </div>
    </div>
</section>

<style>
.messaging-header {
    background: var(--navy);
    padding: 4rem 0 3rem;
    text-align: center;
    border-bottom: 3px solid var(--gold);
}

.messaging-bg {
    position: relative;
    background-image: url('/images/satellites-communication.jpg');
    background-size: cover;
    background-position: center;
    background-attachment: scroll;
}

.messaging-bg::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(20, 28, 43, 0.85);
    z-index: 1;
}

.messaging-bg .container {
    position: relative;
    z-index: 2;
}

.messaging-header-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 1rem;
}

.messaging-header-subtitle {
    font-size: 1.2rem;
    color: rgba(255, 255, 255, 0.9);
    max-width: 700px;
    margin: 0 auto 2rem;
}

.btn-compose-new {
    padding: 1rem 2.5rem;
    background: linear-gradient(135deg, var(--gold) 0%, #f4c542 100%);
    color: var(--navy);
    border: none;
    border-radius: 12px;
    font-weight: 700;
    font-size: 1.1rem;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    transition: all 0.3s;
    box-shadow: 0 4px 12px rgba(212, 175, 55, 0.3);
    margin-bottom: 2rem;
}

.btn-compose-new:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4);
}

.btn-compose-new svg {
    width: 22px;
    height: 22px;
}

.messaging-quick-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    max-width: 800px;
    margin: 0 auto;
}

.msg-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
}

.msg-stat-icon {
    width: 48px;
    height: 48px;
    color: var(--gold);
}

.msg-stat-value {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--gold);
}

.msg-stat-label {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.9rem;
}

@media (max-width: 767px) {
    .messaging-header-title {
        font-size: 2rem;
    }

    .messaging-quick-stats {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
}
</style>
