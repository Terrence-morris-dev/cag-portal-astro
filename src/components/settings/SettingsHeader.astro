<!-- Settings Header -->
<section class="settings-header">
    <div class="container">
        <div class="header-content">
            <div class="header-text">
                <h1 class="settings-title">Account Settings</h1>
                <p class="settings-subtitle">Manage your profile, security, and preferences</p>
            </div>
            <div class="header-meta">
                <span class="last-updated">Last updated: November 7, 2025</span>
            </div>
        </div>
    </div>
</section>

<style>
.settings-header {
    background: var(--navy);
    padding: 3rem 0 2rem;
    border-bottom: 3px solid var(--gold);
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.header-text {
    flex: 1;
}

.settings-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 0.5rem;
}

.settings-subtitle {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
}

.header-meta {
    text-align: right;
}

.last-updated {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.9rem;
    font-style: italic;
}

@media (max-width: 767px) {
    .settings-header {
        padding: 2rem 0 1.5rem;
    }

    .header-content {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
    }

    .settings-title {
        font-size: 2rem;
    }

    .settings-subtitle {
        font-size: 1rem;
    }

    .header-meta {
        text-align: left;
    }
}
</style>
